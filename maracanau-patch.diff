diff --git a/maracanau.html b/maracanau.html
index f80b886..cc9e58f 100644
--- a/maracanau.html
+++ b/maracanau.html
@@ -459,6 +459,12 @@ th.sortable:hover{color:var(--brand);background:rgba(124,58,237,.04)}
 th.sortable .sort-icon{font-size:10px;color:var(--muted);opacity:.45;margin-left:4px}
 th.sort-asc .sort-icon,th.sort-desc .sort-icon{opacity:1;color:var(--brand)}
 th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
+th.sortable[title]::after{content:''}
+@media(hover:hover){
+  th.sortable:not(.sort-asc):not(.sort-desc) .sort-hint::after{
+    content:'clique para ordenar';
+  }
+}
 .mei-high td:nth-child(4){color:#B07C20;font-weight:700}
 
 /* Comparison table */
@@ -1278,6 +1284,8 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
     <!-- KPIs resumo economia -->
     <div class="kpi-grid four" id="kpi-econ-resumo" style="margin-bottom:4px"></div>
 
+    <hr style="border:none;border-top:1px solid var(--line-2);margin:12px 0 0">
+
     <div class="decision" style="margin:12px 0 16px">
       <h3>Destaques econômicos</h3>
       <p>Maracanaú apresenta forte concentração econômica no eixo Pajuçara–Jereissati e no Distrito Industrial. Enquanto o Distrito Industrial abriga empresas com capital social médio na casa dos milhões, bairros como Piratininga, Novo Oriente e Timbó registram quase exclusivamente MEIs, com participação superior a 90%. A taxa média de MEI nos bairros residenciais (>80%) contrasta com menos de 40% no Distrito Industrial — evidência de uma economia dual. Essa assimetria demanda estratégias diferenciadas: programas de formalização e crédito para pequenos negócios na periferia, e articulação com o setor formal no Distrito Industrial para geração de emprego e inclusão produtiva das populações vulneráveis do entorno.</p>
@@ -1362,7 +1370,7 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
               </span>
             </span>
           </h3>
-          <p class="panel-sub">1.175 estabelecimentos ativos · CNAE alimentício (Receita Federal, 2024) · Filtros: categoria, tipo (matriz/filial) e bairro</p>
+          <p class="panel-sub">1.175 estabelecimentos ativos · CNAE alimentício (Receita Federal, 2024) · Filtros: categoria, tipo (matriz/filial), bairro e região</p>
         </div>
       </div>
       <div class="map-analytics-layout">
@@ -1385,6 +1393,9 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
             <select id="aliment-bairro-filter" title="Filtrar por bairro">
               <option value="">Todos os bairros</option>
             </select>
+            <select id="aliment-regiao-filter" title="Filtrar por região">
+              <option value="">Todas as regiões</option>
+            </select>
             <button class="mge-clear-btn" id="aliment-clear-filters" title="Limpar filtros" style="display:none">✕ Limpar</button>
             <span class="mge-filter-badge" id="aliment-filter-count" style="display:none"></span>
           </div>
@@ -1635,11 +1646,23 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
         <div class="chart-box"><canvas id="chart-osc-area" class="chart tall"></canvas></div>
       </div>
       <div class="panel">
-        <div class="panel-head"><div><h3 class="panel-title">OSCs por bairro (IPEA, 2024)</h3><p class="panel-sub">Quantidade de organizações ativas por bairro</p></div></div>
+        <div class="panel-head"><div><h3 class="panel-title">OSCs por bairro (IPEA, 2024)</h3><p class="panel-sub">Quantidade de organizações ativas por bairro (top 25)</p></div></div>
         <div class="chart-box"><canvas id="chart-osc-bairro" class="chart tall"></canvas></div>
       </div>
     </div>
 
+    <!-- OSC por região e densidade -->
+    <div class="grid two" style="margin-top:16px">
+      <div class="panel">
+        <div class="panel-head"><div><h3 class="panel-title">OSCs por região administrativa (IPEA, 2024)</h3><p class="panel-sub">Distribuição das organizações por região</p></div></div>
+        <div class="chart-box"><canvas id="chart-osc-regiao" class="chart"></canvas></div>
+      </div>
+      <div class="panel">
+        <div class="panel-head"><div><h3 class="panel-title">Densidade de OSCs por mil habitantes (IPEA, 2024)</h3><p class="panel-sub">Razão entre organizações ativas e população residente por região</p></div></div>
+        <div class="chart-box"><canvas id="chart-osc-dens" class="chart"></canvas></div>
+      </div>
+    </div>
+
     <div style="margin:24px 0 6px;padding:0 4px">
       <div style="display:flex;align-items:center;gap:12px">
         <span style="font-size:11px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.08em">Tabela detalhada</span>
@@ -1705,6 +1728,28 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
       </div>
     </div>
 
+    <!-- 2b — Comparativo visual entre regiões -->
+    <div style="margin:24px 0 6px;padding:0 4px">
+      <div style="display:flex;align-items:center;gap:12px">
+        <span style="font-size:11px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.08em">Comparativo visual</span>
+        <span style="flex:1;height:1px;background:var(--line-2)"></span>
+      </div>
+    </div>
+    <div class="decision" style="margin:8px 0 14px">
+      <h3>Como ler os gráficos</h3>
+      <p>Os gráficos abaixo comparam indicadores-chave entre as 6 regiões de Maracanaú. A <strong>barra agrupada</strong> permite visualizar rapidamente onde cada região se destaca ou apresenta déficit. A <strong>renda per capita</strong> e a <strong>cobertura de esgoto</strong> indicam infraestrutura e capacidade econômica; a <strong>extrema pobreza</strong> e o <strong>CadÚnico</strong> medem a demanda por proteção social; as <strong>OSCs</strong> e <strong>empresas</strong> refletem a densidade institucional e produtiva. O contraste revela o gradiente de vulnerabilidade do município e orienta a priorização de intervenções.</p>
+    </div>
+    <div class="grid two" style="margin-top:12px">
+      <div class="panel">
+        <div class="panel-head"><div><h3 class="panel-title">Indicadores sociais por região</h3><p class="panel-sub">Extrema pobreza, CadÚnico, analfabetismo e informalidade (médias ponderadas)</p></div></div>
+        <div class="chart-box"><canvas id="chart-synth-social" class="chart"></canvas></div>
+      </div>
+      <div class="panel">
+        <div class="panel-head"><div><h3 class="panel-title">Indicadores econômicos e institucionais</h3><p class="panel-sub">Renda média, empresas, OSCs e cobertura de esgoto</p></div></div>
+        <div class="chart-box"><canvas id="chart-synth-econ" class="chart"></canvas></div>
+      </div>
+    </div>
+
     <!-- 3 — Comparativo por bairro -->
     <div class="panel" style="margin-top:20px">
       <div class="panel-head">
@@ -1924,7 +1969,7 @@ th.sort-asc,th.sort-desc{background:rgba(124,58,237,.06)}
               <td><span class="region-label"><span class="region-dot" style="background:#DC2626"></span>Zona Sul</span></td>
               <td class="cell medio"><span>Médio</span></td>
               <td class="cell baixo"><span>Baixo</span></td>
-              <td class="cell baixo"><span>Alto</span></td>
+              <td class="cell alto"><span>Alto</span></td>
               <td class="cell baixo"><span>Baixo</span></td>
               <td class="cell medio"><span>Médio</span></td>
             </tr>
@@ -2899,6 +2944,7 @@ function initAlimentMap(){
   const selCat = document.getElementById('aliment-cat-filter');
   const selTipo = document.getElementById('aliment-tipo-filter');
   const selBairro = document.getElementById('aliment-bairro-filter');
+  const selRegiao = document.getElementById('aliment-regiao-filter');
   const inpSearch = document.getElementById('aliment-map-search');
   const btnClear = document.getElementById('aliment-clear-filters');
   const badge = document.getElementById('aliment-filter-count');
@@ -2911,6 +2957,12 @@ function initAlimentMap(){
     opt.value = b; opt.textContent = `${b} (${totalByBairro[b]})`;
     selBairro.appendChild(opt);
   });
+  const regioes = [...new Set(Object.values(REGION_MAP))].sort((a,b)=>a.localeCompare(b,'pt-BR'));
+  regioes.forEach(r=>{
+    const opt = document.createElement('option');
+    opt.value = r; opt.textContent = r;
+    selRegiao.appendChild(opt);
+  });
 
   // 3) Build map
   if(!MAPS['map-aliment']){
@@ -2953,11 +3005,13 @@ function initAlimentMap(){
     const cf = selCat.value;
     const tf = selTipo.value;
     const bf = selBairro.value;
+    const rgf = selRegiao.value;
     const sq = (inpSearch.value||'').trim().toLowerCase();
     let out = ALIMENT_POINTS;
     if(cf) out = out.filter(p=>p.cat===cf);
     if(tf) out = out.filter(p=>p.tipo===tf);
     if(bf) out = out.filter(p=>p.b===bf);
+    if(rgf) out = out.filter(p=>(REGION_MAP[p.b]||'')===rgf);
     if(sq) out = out.filter(p=>(p.n||'').toLowerCase().includes(sq) || (p.rf||'').toLowerCase().includes(sq) || (p.id||'').includes(sq));
     return out;
   }
@@ -3026,20 +3080,21 @@ function initAlimentMap(){
     listEl.innerHTML = `<div style="margin-bottom:8px">${catRows}</div><div style="font-size:10px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;padding:6px 8px 4px;border-top:1px dashed var(--line-2)">Lista (${Math.min(listVisible,filtered.length)} de ${filtered.length})</div>${rows}${more}`;
 
     // Filter badge
-    const active = (selCat.value?1:0) + (selTipo.value?1:0) + (selBairro.value?1:0) + (inpSearch.value?1:0);
+    const active = (selCat.value?1:0) + (selTipo.value?1:0) + (selBairro.value?1:0) + (selRegiao.value?1:0) + (inpSearch.value?1:0);
     badge.style.display = active ? 'inline-block' : 'none';
     btnClear.style.display = active ? 'inline-flex' : 'none';
     if(active) badge.textContent = `${active} filtro${active>1?'s':''}`;
-    [selCat,selTipo,selBairro].forEach(s=>s.classList.toggle('active-filter',!!s.value));
+    [selCat,selTipo,selBairro,selRegiao].forEach(s=>s.classList.toggle('active-filter',!!s.value));
     inpSearch.classList.toggle('active-filter', !!inpSearch.value);
   }
 
   selCat.addEventListener('change', renderAlimentMap);
   selTipo.addEventListener('change', renderAlimentMap);
   selBairro.addEventListener('change', renderAlimentMap);
+  selRegiao.addEventListener('change', renderAlimentMap);
   inpSearch.addEventListener('input', renderAlimentMap);
   btnClear.addEventListener('click', ()=>{
-    selCat.value=''; selTipo.value=''; selBairro.value=''; inpSearch.value='';
+    selCat.value=''; selTipo.value=''; selBairro.value=''; selRegiao.value=''; inpSearch.value='';
     renderAlimentMap();
   });
 
@@ -3378,6 +3433,34 @@ function initOsc(){
     }
   }
 
+  // ── OSC por região e densidade ──
+  const regOscCount={};
+  const regPop={};
+  OSCS.forEach(o=>{const r=REGION_MAP[o.bairro]||'Sem região';regOscCount[r]=(regOscCount[r]||0)+1;});
+  DATA.forEach(d=>{const r=REGION_MAP[d.b]||'Sem região';regPop[r]=(regPop[r]||0)+(d.pop||0);});
+  const regLabels=Object.keys(regOscCount).filter(r=>r!=='Sem região').sort((a,b)=>regOscCount[b]-regOscCount[a]);
+  const regValues=regLabels.map(r=>regOscCount[r]);
+  const regColors=regLabels.map(r=>REGION_COLORS[r]||'rgba(124,58,237,.5)');
+  destroyChart('chart-osc-regiao');
+  const cReg=document.getElementById('chart-osc-regiao');
+  if(cReg){
+    const h=Math.max(regLabels.length*38,180);
+    cReg.style.height=h+'px';
+    cReg.parentElement.style.height='auto';
+    CHARTS['chart-osc-regiao']=new Chart(cReg,{type:'bar',data:{labels:regLabels,datasets:[{data:regValues,backgroundColor:regColors,borderRadius:5}]},options:{indexAxis:'y',responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx=>ctx.raw+' OSCs'}}},scales:{x:{grid:{display:false}},y:{grid:{display:false},ticks:{font:{size:11}}}}}});
+  }
+  destroyChart('chart-osc-dens');
+  const cDens=document.getElementById('chart-osc-dens');
+  if(cDens){
+    const densLabels=regLabels.filter(r=>regPop[r]>0);
+    const densValues=densLabels.map(r=>+((regOscCount[r]/(regPop[r]||1))*1000).toFixed(2));
+    const densColors=densLabels.map(r=>REGION_COLORS[r]||'rgba(124,58,237,.5)');
+    const h=Math.max(densLabels.length*38,180);
+    cDens.style.height=h+'px';
+    cDens.parentElement.style.height='auto';
+    CHARTS['chart-osc-dens']=new Chart(cDens,{type:'bar',data:{labels:densLabels,datasets:[{data:densValues,backgroundColor:densColors,borderRadius:5}]},options:{indexAxis:'y',responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx=>ctx.parsed.x.toFixed(2)+' OSCs/mil hab'}}},scales:{x:{grid:{display:false}},y:{grid:{display:false},ticks:{font:{size:11}}}}}});
+  }
+
   // ── Tabela completa de OSCs ──
   let oscTableSort='nome',oscTableDir=-1;
   function renderOscTable(){
@@ -4208,11 +4291,61 @@ function initComp(){
     ].map(k=>`<div class="kpi"><div class="label">${k.label}</div><div class="value">${k.value}</div><div class="sub">${k.sub}</div></div>`).join('');
   }
 
+  // ─── 2b) Gráficos comparativos por região ──────────────────────────────
+  function renderSynthCharts(){
+    const labels = GROUPS;
+    const colors = labels.map(g=>GROUP_COLORS[g]||'#888');
+
+    destroyChart('chart-synth-social');
+    const cSocial = document.getElementById('chart-synth-social');
+    if(cSocial){
+      cSocial.style.height = '300px';
+      cSocial.parentElement.style.height = 'auto';
+      const socialInds = [
+        {k:'epob',label:'Ext. Pobreza'},
+        {k:'cad',label:'CadÚnico'},
+        {k:'anal',label:'Analfabetismo'},
+        {k:'meiShare',label:'% MEI'}
+      ];
+      CHARTS['chart-synth-social']=new Chart(cSocial,{type:'bar',data:{
+        labels:socialInds.map(i=>i.label),
+        datasets:labels.map((g,gi)=>({
+          label:g,
+          data:socialInds.map(i=>(groupAgg[g].values[i.k]||0)*100),
+          backgroundColor:colors[gi]+'CC',
+          borderRadius:4
+        }))
+      },options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'bottom',labels:{font:{size:10},boxWidth:10}},tooltip:{callbacks:{label:ctx=>ctx.dataset.label+': '+ctx.parsed.y.toFixed(1)+'%'}}},scales:{x:{grid:{display:false},ticks:{font:{size:10}}},y:{grid:{color:'#F0F0F0'},ticks:{callback:v=>v+'%'}}}}});
+    }
+
+    destroyChart('chart-synth-econ');
+    const cEcon = document.getElementById('chart-synth-econ');
+    if(cEcon){
+      cEcon.style.height = '300px';
+      cEcon.parentElement.style.height = 'auto';
+      const econInds = [
+        {k:'rendaPC',label:'Renda PC (R$)'},
+        {k:'emp',label:'Empresas'},
+        {k:'osc',label:'OSCs'}
+      ];
+      CHARTS['chart-synth-econ']=new Chart(cEcon,{type:'bar',data:{
+        labels:econInds.map(i=>i.label),
+        datasets:labels.map((g,gi)=>({
+          label:g,
+          data:econInds.map(i=>{const v=groupAgg[g].values[i.k]||0;return Math.round(v);}),
+          backgroundColor:colors[gi]+'CC',
+          borderRadius:4
+        }))
+      },options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'bottom',labels:{font:{size:10},boxWidth:10}},tooltip:{callbacks:{label:ctx=>ctx.dataset.label+': '+Number(ctx.parsed.y).toLocaleString('pt-BR')}}},scales:{x:{grid:{display:false},ticks:{font:{size:10}}},y:{grid:{color:'#F0F0F0'}}}}});
+    }
+  }
+
   // ─── Execução ──────────────────────────────────────────────────────────────
   renderDivisionMap();
   renderGroupCards();
   renderRegionTable();
   renderBairroTable();
+  renderSynthCharts();
   renderRadar();
   renderReading();
   renderHeatmap();
